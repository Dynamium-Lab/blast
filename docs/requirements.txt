# Documentation build dependencies (Sphinx + extensions).
# The `blast` Python package itself is installed separately (pip install .)
# so that autodoc can import the compiled extension.
sphinx>=7.2
furo>=2024.1.29
myst-parser>=2.0
breathe>=4.35
